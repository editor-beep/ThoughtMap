# URL of the deployed backend (e.g. https://your-app.vercel.app)
# For local dev: use your machine's LAN IP, not localhost
#   macOS/Linux: ip addr / ifconfig
#   Windows: ipconfig
# Example: http://192.168.1.42:5000
EXPO_PUBLIC_API_URL=
